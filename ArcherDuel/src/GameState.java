/**
 * Defines the various states the game can be in.
 */
public enum GameState {
    LOADING,
    MAIN_MENU,
    MAP_SELECT,
    CONTROLS,
    PLAYING,
    SLOW_MO,
    OVER,
    PAUSED,
    SETTINGS
}
